# =================================================
# 🏗️ Easy Table Converter 环境变量配置
# 生产部署前请确保所有必要配置正确设置
# =================================================

# ------------------------------
# 🖥️ 站点基础配置
# ------------------------------
# 站点公开URL（用于客户端访问、OAuth回调等）
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# 品牌名称
BRAND_NAME=YYC³

# 调试模式（生产环境必须设置为false）
DEBUG_MODE=false

# ------------------------------
# 🔒 安全配置
# ------------------------------
# 环境变量严格模式（生产环境建议开启）
ENV_STRICT=true

# 可选的第三方服务API密钥
# API_KEY=your-api-key

# ------------------------------
# ⚡ API配置（所有数值均为字符串形式）
# ------------------------------
# 最大上传文件大小（字节），默认10MB
# API_MAX_UPLOAD_SIZE=10485760

# API超时时间（毫秒），默认30s
# API_TIMEOUT_MS=30000

# 速率限制窗口（毫秒），默认60s
# API_RATE_WINDOW_MS=60000

# IP速率限制最大请求数，默认30
# API_RATE_MAX_PER_IP=30

# IP并发请求限制，默认2
# API_CONCURRENCY_MAX_PER_IP=2

# 全局并发请求限制，默认8
# API_CONCURRENCY_MAX_TOTAL=8

# 队列软限制，默认50
# API_QUEUE_SOFT_LIMIT=50

# ------------------------------
# 🖼️ 图片配置
# ------------------------------
# 允许的图片输出格式（逗号分隔）
# IMAGE_ALLOWED_OUTPUT_FORMATS=png,jpeg,webp,avif,tiff,heif

# ------------------------------
# 📊 监控配置
# ------------------------------
# 启用性能监控（默认false）
# ENABLE_MONITORING=false

# ------------------------------
# 📝 日志配置
# ------------------------------
# 日志级别（error,warn,info,debug）
# LOG_LEVEL=info

# ------------------------------
# 注意事项
# ------------------------------
# 1. 生产环境请复制此文件为.env.local并填入实际值
# 2. 敏感信息（如API密钥）请勿提交到代码仓库
# 3. 配置值超出边界时，系统会自动调整并发出告警
# 4. 详细配置说明请参考项目文档