# =================================================
# 🏗️ Easy Table Converter 生产环境配置示例
# 部署时请根据实际环境修改
# =================================================

# ------------------------------
# 🖥️ 站点基础配置
# ------------------------------
# 站点正式域名（用于 metadataBase、OG/Twitter 等）
NEXT_PUBLIC_SITE_URL=https://yyc3.example.com

# 品牌名称
BRAND_NAME=YYC³

# 调试模式（生产环境必须设置为false）
DEBUG_MODE=false

# ------------------------------
# 🔒 安全配置
# ------------------------------
# 环境变量严格模式（生产环境必须开启）
ENV_STRICT=true

# 第三方服务API密钥（示例）
# API_KEY=prod-api-key-1234567890

# ------------------------------
# ⚡ API配置（生产环境推荐值）
# ------------------------------
# 最大上传文件大小（字节），生产环境建议20MB
API_MAX_UPLOAD_SIZE=20971520

# API超时时间（毫秒），生产环境建议60s
API_TIMEOUT_MS=60000

# 速率限制窗口（毫秒），默认60s
API_RATE_WINDOW_MS=60000

# IP速率限制最大请求数，生产环境建议50
API_RATE_MAX_PER_IP=50

# IP并发请求限制，生产环境建议3
API_CONCURRENCY_MAX_PER_IP=3

# 全局并发请求限制，生产环境建议16
API_CONCURRENCY_MAX_TOTAL=16

# 队列软限制，生产环境建议100
API_QUEUE_SOFT_LIMIT=100

# ------------------------------
# 🖼️ 图片配置
# ------------------------------
# 允许的图片输出格式（逗号分隔）
IMAGE_ALLOWED_OUTPUT_FORMATS=png,jpeg,webp,avif

# ------------------------------
# 📊 监控配置
# ------------------------------
# 启用性能监控（生产环境建议开启）
# ENABLE_MONITORING=true

# ------------------------------
# 📝 日志配置
# ------------------------------
# 日志级别（生产环境建议warn）
LOG_LEVEL=warn

# ------------------------------
# 部署注意事项
# ------------------------------
# 1. 请确保设置正确的域名，这会影响所有外部链接和OAuth回调
# 2. 根据服务器性能调整并发和队列参数
# 3. 敏感信息请通过环境变量注入，不要硬编码
# 4. 建议定期备份此配置文件
# 5. 部署前请测试所有配置项是否正常工作
